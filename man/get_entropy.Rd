% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_entropy.R
\name{get_entropy}
\alias{get_entropy}
\title{Compute entropy of infector assignments}
\usage{
get_entropy(out, burnin = 0, normalise = TRUE)
}
\arguments{
\item{out}{An object of class \code{outbreaker_chains}.}

\item{burnin}{The number of iterations to be discarded as burnin.}

\item{normalise}{Logical. If \code{TRUE} (default), entropy is normalised
to \eqn{[0, 1]} by dividing by \code{log(K)}, where \code{K} is the number
of distinct inferred infectors.}
}
\value{
A named numeric vector of entropy values, one per case.
}
\description{
Computes the Shannon entropy of inferred infectors for each case across
posterior samples. Entropy quantifies uncertainty in infector assignment:
0 means complete certainty, 1 means maximum uncertainty.
}
\examples{
\dontrun{
data(fake_outbreak)
out <- outbreaker(
  data = outbreaker_data(
    dates = fake_outbreak$onset,
    dna = fake_outbreak$dna,
    w_dens = fake_outbreak$w
  ),
  config = create_config(n_iter = 500, sample_every = 50)
)
get_entropy(out)
}

}
\seealso{
\code{\link{get_Ri}}, \code{\link{outbreaker_chains}}.
}
\author{
Cyril Geismar (\email{c.geismar21@imperial.ac.uk}).
}
