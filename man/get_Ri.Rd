% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_Ri.R
\name{get_Ri}
\alias{get_Ri}
\title{Compute case reproduction numbers}
\usage{
get_Ri(
  out,
  burnin = 0,
  raw = FALSE,
  stats = list(mean = mean, lwr = function(x) quantile(x, 0.025, na.rm = TRUE), upr =
    function(x) quantile(x, 0.975, na.rm = TRUE))
)
}
\arguments{
\item{out}{An object of class \code{outbreaker_chains}.}

\item{burnin}{The number of iterations to be discarded as burnin.}

\item{raw}{Logical. If \code{TRUE}, returns a long-format data frame with
columns \code{step}, \code{case}, and \code{ri}. If \code{FALSE}
(default), returns summary statistics per case.}

\item{stats}{A named list of summary functions applied across steps.
Ignored when \code{raw = TRUE}.}
}
\value{
When \code{raw = FALSE}, a data frame with columns \code{case},
  \code{mean}, \code{lwr}, \code{upr}. When \code{raw = TRUE}, a data
  frame with columns \code{step}, \code{case}, \code{ri}.
}
\description{
Computes the number of secondary infections caused by each case across
posterior samples. For each MCMC step, counts how many times each case
appears as an infector in the \code{alpha} columns.
}
\examples{
\dontrun{
data(fake_outbreak)
out <- outbreaker(
  data = outbreaker_data(
    dates = fake_outbreak$onset,
    dna = fake_outbreak$dna,
    w_dens = fake_outbreak$w
  ),
  config = create_config(n_iter = 500, sample_every = 50)
)
get_Ri(out)
get_Ri(out, raw = TRUE)
}

}
\seealso{
\code{\link{get_offspring}}, \code{\link{get_trees}},
  \code{\link{outbreaker_chains}}.
}
\author{
Cyril Geismar (\email{c.geismar21@imperial.ac.uk}).
}
