% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_si.R
\name{get_si}
\alias{get_si}
\title{Compute serial interval distribution}
\usage{
get_si(
  out,
  data,
  burnin = 0,
  raw = FALSE,
  stats = list(mean = mean, lwr = function(x) quantile(x, 0.025, na.rm = TRUE), upr =
    function(x) quantile(x, 0.975, na.rm = TRUE))
)
}
\arguments{
\item{out}{An object of class \code{outbreaker_chains}.}

\item{data}{An \code{outbreaker_data} object containing the onset dates.}

\item{burnin}{The number of iterations to be discarded as burnin.}

\item{raw}{Logical. If \code{TRUE}, returns a long-format data frame with
columns \code{step}, \code{x}, and \code{y}. If \code{FALSE} (default),
returns summary statistics across steps.}

\item{stats}{A named list of summary functions applied across steps.
Ignored when \code{raw = TRUE}.}
}
\value{
When \code{raw = FALSE}, a data frame with columns \code{x} (serial
  interval value), \code{mean}, \code{lwr}, \code{upr}. When
  \code{raw = TRUE}, a data frame with columns \code{step}, \code{x},
  \code{y}.
}
\description{
Computes the serial interval distribution across posterior samples. The
serial interval is the time between onset dates in each infector-infectee
pair. For each MCMC step, transmission pairs are identified from the
\code{alpha} columns and onset-to-onset delays are computed using the dates
in the \code{outbreaker_data} object.
}
\examples{
\dontrun{
data(fake_outbreak)
dat <- outbreaker_data(
  dates = fake_outbreak$onset,
  dna = fake_outbreak$dna,
  w_dens = fake_outbreak$w
)
out <- outbreaker(
  data = dat,
  config = create_config(n_iter = 500, sample_every = 50)
)
get_si(out, dat)
get_si(out, dat, raw = TRUE)
}

}
\seealso{
\code{\link{get_trees}}, \code{\link{outbreaker_chains}}.
}
\author{
Cyril Geismar (\email{c.geismar21@imperial.ac.uk}).
}
